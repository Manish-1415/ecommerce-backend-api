import { IUser } from "./user.types";
import mongoose from "mongoose";

const UserService = {
    registerUser : async (userData : IUser) : Promise<void>=> {
        try {


        } catch (error) {
            
        }
    }
}



export default UserService;